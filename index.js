const number = [1,2,3,4];

const output = move(numbers, 1,-2);

console.log(output);

function move(array, index, offset) {
    const output = [];
    for (let value in array) {
        if (value.indexOf() !== index) output.push(value);
        else if
    }

}